[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  SECRETS_SCAN_OMIT_PATHS = "firebase-applet-config.json"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
